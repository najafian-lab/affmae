"""Utilities: checkpoints, config, distribution, geometry, masking, paths.

Deliberately empty of re-exports. ``affmae.viz`` imports matplotlib,
so re-exporting it here would make every ``affmae.utils`` import pull the
plotting stack.
"""
